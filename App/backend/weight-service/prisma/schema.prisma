datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Weight {
 id      String   @id @default(uuid()) @db.Uuid
  date    DateTime
  weight  Float
  userId  String   @db.Uuid
}
